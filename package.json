{
  "scripts": {
    "build": "wasm-pack build --target web --no-default-features --features \"wasm\" && rollup --config rollup.config.js"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "@rollup/plugin-commonjs": "^25.0.4",
    "@rollup/plugin-node-resolve": "^15.2.1",
    "rollup": "^v2.79.1",
    "rollup-plugin-base64": "^1.0.1"
  }
}
